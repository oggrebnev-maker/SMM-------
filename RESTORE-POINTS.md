# Точки восстановления

Краткая история изменений. Каждая запись = коммит, залитый на GitHub. При сбое можно откатиться к любому коммиту (например, `git checkout <коммит>`).

---

## 2025-03-11 — Топбар «Настройки проекта»: раскрытие поверх основного окна

**Коммит:** _(указать после коммита)_

- **Окно проекта (≤768px):** раскрывающаяся панель «Настройки проекта» больше не прячется за главным экраном — рисуется поверх. У сайдбара-топбара заданы `z-index: 20` и `overflow: visible`; у выпадающего `.project-view-nav` — `position: absolute`, `top: 100%`, `z-index: 30`, тень и анимация opacity. Клик по заголовку топбара (кроме кнопки-стрелки) тоже переключает раскрытие.
- **Очистка:** удалена отладочная инструментация (fetch-логи) из `initProjectViewModal` в projects.js.

**Файлы:** assets/css/app.css, assets/js/pages/projects.js.

**Восстановление:** `git checkout <коммит>`

---

## 2025-03-11 — Страница проектов: сайдбар, главное окно, карточки, окно удаления

**Коммит:** _(указать после коммита)_

- **Страница проектов:** кнопка «Добавить проект» перенесена в правый сайдбар (под текст), на всю ширину, размер 1; заголовок «Проекты» увеличен (page-sidebar-title--large). На странице списка проектов ни одна карточка не подсвечивается как выбранная (current не передаётся в renderProjectCard).
- **Главное окно:** отступы от сайдбаров 50px (padding: 50px у .page-main); для страницы /projects контейнеру задаётся класс has-right-sidebar (app.js), чтобы у #page-container не было лишних отступов.
- **Карточки проектов:** кнопки «Редактировать» и «Удалить» в стиле сайта (.btn-icon, рамка var(--border), скругление var(--btn-radius)); кнопка «Команда» удалена. Клик по «Редактировать» открывает актуальный редактор (openProjectView), а не старую панель в сайдбаре. Подсказки: «Настройки проекта», «Удалить проект».
- **Удаление проекта:** своё окно подтверждения (projectViewConfirm), не системное confirm. Отработанное всплывающее окно (project-view-confirm-*): восстановлено в JS и CSS, в DESIGN.md п. 5.1 зафиксировано «не заменять на .modal». Заголовок прописными (УДАЛИТЬ ПРОЕКТ «…»?), текст: «Обратите внимание, что это действие необратимо!» и «Вы уверены, что хотите насовсем удалить этот проект?»; кнопки размера 2: сверху красная «Я уверен(а)!», снизу «Не удалять». Ширина окна динамическая (fit-content, max-width 360px), отступы фиксированные. Адаптив (перенос кнопок и т.д.) — в планах на доработку.
- **Файлы:** assets/js/pages/projects.js, assets/js/app.js, assets/css/app.css, DESIGN.md.

**Восстановление:** `git checkout <коммит>`

---

## 2025-03-11 — Адаптив окна проекта, второй топбар, иконки статусов

**Коммит:** _(указать после коммита)_

- **Адаптив:** при сужении первой сжимается основная часть; сайдбар 25% с min-width 240px, доля растёт до 28%/30% на средних ширинах; с 768px колонка, сайдбар сверху (order).
- **Второй топбар (≤768px):** сайдбар свёрнут в шапку «Настройки проекта» + иконка раскрытия; по клику раскрывается навигация; при выборе пункта меню автоматически сворачивается.
- **Иконки статусов (≤800px):** тексты «добавлены»/«настроено» заменены на зелёную галочку, противоположные — на красный крест.
- Кнопка: «Аккаунты проекта» → «Аккаунты»; правки адаптива модалок, тостов, формы поста.

**Подробности:**
- Адаптив окна: `.project-view-main` — `flex: 1 1 0`, `min-width: 0`; сайдбар не ниже 240px; до 1100px — 28%, до 1000px — 30%; панели (настройки, UTM) 380px; с 768px колонка (order -1 / 1).
- Второй топбар: `.project-view-sidebar-header` + `.project-view-sidebar-toggle`; класс `project-view-sidebar-expanded`; сворачивание в `switchProjectViewSection`.
- Иконки статусов: класс `.is-set` у `.project-settings-nav-status`; SVG `.project-settings-nav-cross`; медиазапрос 800px.
- Файлы: `assets/js/pages/projects.js`, `assets/css/app.css`.

**Восстановление:** `git checkout <коммит>`

---

## 2025-03-11 — Вотермарк, хэштеги: единый бейдж и Drag&Drop

**Коммит:** _(указать после коммита)_

- **Вотермарк:** зона загрузки как в форме поста (Креативы), одна миниатюра; сохранение без перезагрузки превью; `GET /api/projects/{id}/watermark/image` для отдачи картинки; миграция колонок вотермарка в `projects`; делегирование клика Save на секцию.
- **Хэштеги:** иконка Drag&Drop (шесть точек) перенесена внутрь бейджа тега перед `#`; отображение `#тег` без `::`; единый визуальный блок на тег.

**Восстановление:** `git checkout <коммит>`

---

## 2025-03-11 — Журнал точек восстановления

**Коммит:** `3587bd4`

- Добавлен файл `RESTORE-POINTS.md`: с этого момента каждая новая точка восстановления будет кратко описываться здесь.

**Восстановление:** `git checkout 3587bd4`

---

## 2025-03-11 — UI, кнопки, сайдбар/топбар; display_id отложен

**Коммит:** `34a9ebb`

- Обновлён UI: сайдбар (иконки/меню), топбар, мобильный drawer, выбор проекта.
- Введена система кнопок (см. `DESIGN.md`).
- Фича «человекочитаемый ID пользователя» (display_id) отложена: в репо добавлены миграция SQL, `UserIdService`, скрипт миграции пользователей — логика в приложении отключена, вход через соцсети работает.
- Убрана отладочная инструментация.

**Восстановление:** `git checkout 34a9ebb`

---

## 2025-03-11 — Расписание в проекте, модалки, блюр, кнопки, время

**Коммит:** `f74314c`

- **Расписание в настройках проекта:** секция на всю ширину, подпись заменена на «Укажите время публикации постов проекта…», кнопка «Очистить расписание» — красная (btn-danger).
- **Модалки подтверждения:** раздельные заголовки и тексты для «Удалить аккаунт из проекта» и «Очистить расписание»; под удаление аккаунта — текст про раздел «Аккаунты».
- **Блюр под всплывающими окнами:** у модалки подтверждения (project-view-confirm-backdrop) и у модалки «Добавить кнопку» (posts-add-button-modal-backdrop) добавлен backdrop-filter: blur(4px).
- **Выбор времени в расписании:** стрелки размещены ровно над числами (две колонки: часы и минуты с up/число/down в каждой).
- **Бэкенд/API:** PATCH для каналов проекта (is_active), API.patch в api.js.

**Восстановление:** `git checkout f74314c`

---

## 2025-03-11 — UTM-метки в настройках проекта

**Коммит:** `5ac9059`

- **Раздел UTM в окне проекта:** вместо плейсхолдера добавлен полноценный блок с заголовком, описанием и чекбоксом «Включить UTM генератор».
- **Поля меток:** utm_campaign, utm_content, utm_medium, utm_source, utm_term с плейсхолдерами по динамическим подстановкам ({page_id}, {post_num}, {page_name}, {soc_net}, {post_datetime}).
- **Подсказки по UTM:** общий «?» сверху с расшифровкой динамических меток; для campaign/content/medium/source/term — отдельные «?» рядом с заголовком, разворачивающие текст с примерами. Открыта может быть только одна подсказка одновременно.
- **Статус в сайдбаре:** «UTM метки» помечаются как настроенные, если генератор включён и заполнено хотя бы одно поле; данные сохраняются в utm_template проекта.

**Восстановление:** `git checkout 5ac9059`

---

## 2025-03-17 — Сайдбары разделов, очистка отладки

**Коммит:** `6f959d4`

- **Сайдбары страниц:** добавлена централизованная настройка заголовков/подсказок правых сайдбаров в «Настройках системы» → вкладка «Сайдбары». Список страниц соответствует левому меню (Дашборд, Контент, Аналитика, Шаблоны, Проекты, Аккаунты, Команды, ИИ Ассистент + служебные страницы Расписание публикаций, Профиль, Настройки системы).
- **Фронтенд:** страницы `dashboard`, `profile`, `social-accounts`, `publish-schedule`, `system-settings` берут тексты сайдбаров из глобальных публичных настроек (`State.publicSettings` → `/api/system-settings/public` / `/api/system-settings`).
- **Бэкенд:** в `SystemSettingsController` добавлены ключи `sidebar_*` для всех пунктов навигации и правых сайдбаров; публичный endpoint `/system-settings/public` отдаёт эти значения.
- **Чистка отладки:** удалены временные HTTP-логгеры и файловые логи (`debug-f3b80a.log`, `debug-9224db.log`, старый `cleanup.log`), чтобы репозиторий содержал только рабочий код.

**Восстановление:** `git checkout 6f959d4`

---

## 2025-03-17 — Раздел «Контент»: маршрут, сайдбар, фильтры

**Коммит:** _(указать после коммита)_

- **Маршрут раздела «Контент»:** вместо `#/posts` используется `#/content` — ссылка в левом меню и роутер SPA (`ROUTES` в `app.js`) обновлены; переходы из дашборда ведут на `/content`.
- **Главное окно:** раздел «Контент» использует тот же layout, что и дашборд/проекты: `#page-container.posts-layout` без внутренних отступов, `.posts-content` растягивается на всю ширину (`padding: 0`, `width: 100%`, `box-sizing: border-box`) и опирается только на отступы `.page-main` по правилам `DESIGN.md`.
- **Правый сайдбар:** `PagePosts.render()` подставляет заголовок и текст из системных настроек (`sidebar_posts_title` / `sidebar_posts_hint` в `State.publicSettings` / `/system-settings.public`), так же как это сделано для Дашборда, Профиля и других страниц; под ними размещено сервисное меню из больших кнопок «Весь контент», «Черновики», «Шаблоны`».
- **Фильтры:** выбор недели/месяца и режима просмотра перенесены из топбара в основную область раздела «Контент» (над календарной сеткой), всегда располагаются в одну строку по левому краю.
- **Календарь:** неделя и месяц используют адаптивную сетку без горизонтального скролла на узких экранах; в ячейках день недели и число объединены в одну подпись.
- **Дизайн:** в `DESIGN.md` задокументированы сервисные кнопки в сайдбаре и добавлен раздел про «Контент» как эталонную страницу с правым сайдбаром.

**Восстановление:** `git checkout <коммит>`
